# 2026-09-06

## Production hardening

- Scrub `.env.example` → placeholders; `!.env.example` ใน gitignore
- `requireAuth` + `ADMIN_EMAILS` / `app_metadata.role === admin`
- `safeNextPath` สำหรับ login `next` + middleware
- Public rate limit (options 30/min, report 5/min) + options `take` 500
- Approve ตรวจ player.memberId; soft duplicate report 2 นาที
- Modal `closeDisabled`; field / search / dashboard limits; CSP headers
- `npm run build:release` = migrate deploy + generate + build
